import * as React from 'react';
import { View, Text } from 'react-native';

function Menu({ navigation }) {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Main Landing Page Contents</Text>
    </View>
  );
}

export default Menu;
